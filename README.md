# Dotfiles for gaming pc

## Enabling systemd networkd and resolvd

1. Enable the services.
1. Edit the dns servers in /etc/systemd/resolved.conf
1. ln -s /usr/lib/systemd/resolv.conf /etc/resolv.conf
